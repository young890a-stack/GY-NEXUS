import { NextRequest,NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
export async function POST(req:NextRequest){try{const body=await req.json(); if(!body.channel) return NextResponse.json({error:"channel is required"},{status:400}); const supabase=createAdminClient(); const {data,error}=await supabase.from("revenue_events").insert({channel:body.channel,content_id:body.contentId||null,title:body.title||null,views:Number(body.views||0),clicks:Number(body.clicks||0),conversions:Number(body.conversions||0),revenue:Number(body.revenue||0),currency:body.currency||"KRW",occurred_at:body.occurredAt||new Date().toISOString(),metadata:body.metadata||{}}).select().single(); if(error) throw error; return NextResponse.json({ok:true,data});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"이벤트 저장 실패"},{status:500})}}
