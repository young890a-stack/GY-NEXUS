import JsonImportBox from "@/components/import/JsonImportBox";
import { createAdminClient } from "@/lib/supabase/admin";
export const dynamic="force-dynamic";
export default async function RevenuePage(){const supabase=createAdminClient();const {data}=await supabase.from("revenue_events").select("*").order("occurred_at",{ascending:false}).limit(100);const total=(data||[]).reduce((s,x)=>s+Number(x.commission||0),0);return <div className="admin-page"><div className="admin-top"><div><span className="eyebrow">REVENUE ANALYTICS</span><h1>매출·수익 분석</h1><p>쿠팡·테무 등 정산 CSV를 JSON으로 변환해 가져오거나 API 연동 결과를 저장합니다.</p></div><div className="panel"><strong>누적 수수료</strong><div style={{fontSize:28,fontWeight:900}}>{total.toLocaleString("ko-KR")}원</div></div></div><JsonImportBox type="revenue"/><section className="panel" style={{marginTop:24}}><div className="table-wrap"><table><thead><tr><th>플랫폼</th><th>주문번호</th><th>매출</th><th>수수료</th><th>일시</th></tr></thead><tbody>{(data||[]).map(x=><tr key={x.id}><td>{x.platform}</td><td>{x.order_id}</td><td>{Number(x.amount).toLocaleString()}원</td><td>{Number(x.commission).toLocaleString()}원</td><td>{new Date(x.occurred_at).toLocaleString("ko-KR")}</td></tr>)}</tbody></table></div></section></div>}
